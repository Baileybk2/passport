<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <link rel="stylesheet" href="/stylesheets/show.css" />
    <title>Edit <%= country.country %></title>
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>
    <h1 class="page-title">Edit <%= country.country %></h1>
    <div class="edit-body">
    <form
      class="form-container"
      action="/users/<%= user._id %>/countries/<%= country._id %>?_method=PUT"
      method="POST"
    >
      <label for="country">Country:</label>
      <input
        type="text"
        name="country"
        id="country"
        value="<%= country.country %>"
      />

      <label for="date">Date:</label>
      <input type="text" name="date" id="date" value="<%= country.date %>" />

      <label for="pointOfEntry">Point of Entry (city):</label>
      <input type="text" name="pointOfEntry" id="pointOfEntry" value="<%= country.pointOfEntry %>"/>

      <label for="purpose">Purpose:</label>
      <select id="purpose" name="purpose">
        <option value="Leisure" <%= country.purpose === "Leisure" ? "selected" : "" %>>Leisure</option>
        <option value="Business" <%= country.purpose === "Business" ? "selected" : "" %>>Business</option>
        <option value="Education" <%= country.purpose === "Education" ? "selected" : "" %>>Education</option>
        <option value="Other" <%= country.purpose === "Other" ? "selected" : "" %>>Other</option>
      </select>

      <label for="notes">Notes:</label>
      <textarea name="notes" id="notes"><%= country.notes %></textarea>

      <label for="locationOfStay">Location of Stay (city):</label>
      <input type="text" name="locationOfStay" id="locationOfStay" value="<%= country.locationOfStay %>"/>

      <button type="submit" class="update-button">Update Country</button>
    </form>
  </div>
  </body>
</html>
